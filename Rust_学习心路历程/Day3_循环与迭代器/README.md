# Day 3 - 循环、迭代器、map/filter/fold

## 今天学了什么
- 三种循环：`loop` / `while` / `for`
- 迭代器基础：`.iter()`
- 链式处理：`map` / `filter` / `fold`

## 心路历程
今天最大的提升是从“会写循环”进入“会做数据流水线”。
对 `map/filter/fold` 不只停留在用法，还追问了借用细节：
为什么 `filter` 常见要解引用，而 `map/fold` 写法不同。

这说明我已经从“记语法”转向“理解底层行为”。
另外，我能主动指出讲解里的不严谨之处，并要求修正，
这让我更确定：**学习质量来自对正确性的坚持。**

## 学习代码
见本目录 `examples.rs`。
